import { styled } from "@mui/material/styles";
import { Button, Typography, InputBase } from "@mui/material";

export const PageTitleLbl = styled(Typography)(({ theme }) => ({
  fontWeight: 400,
  // padding: 19,
  fontSize:28,
  color: theme.palette.common.black,
}));




